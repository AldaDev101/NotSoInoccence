if (!surface_exists(minimap)){
	minimap = surface_create(512,512);
}

surface_set_target(minimap);
draw_sprite(Spr_Mapa_Prueba, 0, -vista_x, -vista_y);

surface_reset_target();

draw_surface(minimap, 882, 1453 );











































































































































































































































































































































































































































































































































































































































































